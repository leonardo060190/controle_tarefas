const path = require('path');//importa a biblioteca de path para trabalhar com pastas

module.exports = {
    'config': path.resolve(__dirname, 'config', 'db.js'),//aponta para aonde esta configurado o banco de dados
    'migrations-path': path.resolve(__dirname, 'migrations')//aponta para a pasta migrations
};